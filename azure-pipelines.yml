# C/C++ with GCC
# Build your C/C++ project with GCC using make.
# Add steps that publish test results, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/c-cpp/gcc

#trigger:
#- main

#jobs:
#- job: c7-build
#  container: centos:7 # Docker Hub image reference
#
#steps:
#- script: |
#    .travis/do.sh
#  displayName: 'make'


jobs:
  - job: ce7
    pool:
      vmImage: 'ubuntu-18.04'
    steps:
    - script: docker pull centos:7
    - script: docker run -it -e LC_ALL=en_US.utf-8 -e LANG=en_US.utf-8 -d -h pbs.dev.local --name pbsdev -v $(pwd):$(pwd) --privileged -w $(pwd) centos:7 /bin/bash
    - script: docker exec -it -e LC_ALL=en_US.utf-8 -e LANG=en_US.utf-8 --privileged pbsdev ./travis/do.sh

